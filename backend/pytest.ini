[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: mark test as a unit test
    integration: mark test as an integration test (Redis/Celery/Ollama/etc.)
    slow: mark test as slow running
filterwarnings =
    ignore::DeprecationWarning
